Related work addresses adjacent parts of the perception-to-contact pipeline along complementary axes. Active-view manipulation methods select additional camera viewpoints to reduce target-pose uncertainty before execution [C1], which is closely related to confidence-triggered view acquisition in our pipeline. Guarded insertion controllers instead focus on the contact phase, using force and pose-deviation checks to limit execution when insertion departs from the planned path [C2]. Language-guided action proposal systems provide another route by using visual and language context to suggest task-relevant object-centric actions [C3]. The present pipeline is positioned at the handoff between these axes: it refines the insertion pose through uncertainty-driven view acquisition before passing the result to guarded contact execution, while treating prior approaches as complementary rather than incorrect.

Citation status: [C1], [C2], and [C3] are synthetic repository-test placeholders tied only to the provided snippets; they should be replaced with source-grounded citations before manuscript use.