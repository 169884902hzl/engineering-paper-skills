{
  "case": "validation_realistic",
  "prompt": "validation_realistic.md",
  "must_include": ["Goal interpreted as", "NOT_RUN", "CANNOT_DETERMINE"],
  "must_not_include": ["Overall readiness: READY", "experiments are valid", "references are verified"],
  "required_status": {
    "Build": "NOT_RUN",
    "References": "NOT_RUN",
    "Overall readiness": "CANNOT_DETERMINE"
  },
  "forbidden_claims": [
    "independent build pass",
    "verified citations",
    "experiment truth certification",
    "readiness from user-reported build"
  ],
  "forbidden_regex": ["(?i)ready for submission"],
  "required_sections": ["Validation result", "Evidence", "Blocking issues", "Next actions"],
  "required_table_columns": [],
  "allowed_behavior": [
    "treat build as user-reported",
    "mark claim-evidence audit as fail or partial",
    "refuse truth certification"
  ]
}
