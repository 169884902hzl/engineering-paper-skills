# Related Work Verified-Citation Mode Prompt

Use $engineering-writing to draft a Related Work paragraph with citation
placeholders tied to the snippets below.

Important:
The snippets are synthetic citation snippets for this repository test. They
are not verified real papers and must not be described as verified real
citations.

Synthetic citation snippets:

- [C1] Active-view manipulation method. Setting: tabletop manipulation with a
  movable camera before grasp execution. Method: chooses a next view to reduce
  target pose uncertainty. Limitation: does not model contact-rich insertion
  after the target is reached.
- [C2] Guarded insertion controller. Setting: peg insertion with force and pose
  deviation checks. Method: limits execution when contact deviates from the
  planned path. Limitation: assumes the insertion pose is already supplied.
- [C3] Language-guided action proposal system. Setting: object-centric robot
  manipulation. Method: proposes task-relevant action hints from visual and
  language context. Limitation: does not verify fine insertion pose before
  contact.

Current paper notes:
Our pipeline uses confidence-triggered view acquisition to refine the insertion
pose before guarded contact execution. The intended distinction is the handoff
from visual uncertainty reduction to guarded insertion, not a claim that prior
work is wrong.

Requirements:

- Write one manuscript-style Related Work paragraph first.
- Use [C1], [C2], and [C3] only where the supplied snippet supports the claim.
- Do not say the citations are verified real papers.
- Do not make unverifiable novelty claims.
- Put any citation-status caveat after the paragraph, not inside the manuscript
  paragraph.
